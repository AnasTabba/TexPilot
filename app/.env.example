# Copy to .env.local (gitignored) and set for your machine.
#
# Where the TexPilot API lives. On a physical phone, 127.0.0.1 is the phone
# itself -- use your laptop's LAN IP and run the API with `make api-lan`.
#   macOS:  ipconfig getifaddr en0
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
