# Copy to .env and fill in. .env is gitignored.
TEXPILOT_ENV=development
TEXPILOT_MODEL_PATH=          # path to trained vision checkpoint; empty = stub predictor (abstains)
TEXPILOT_KB_PATH=services/consistency/kb.yaml
TEXPILOT_OCR_BACKEND=stub     # stub | paddle | vision_api
TEXPILOT_STORAGE_DIR=./data/scans
